
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Tarefa {
  id        Int      @id @default(autoincrement())
  nome      String
  descricao String
  inicio    DateTime
  fim       DateTime
  imagem    String
}
